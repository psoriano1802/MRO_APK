<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>

    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/login_background"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/login_background"
        android:supportsRtl="true"
        android:theme="@style/Theme.Agroag_mro"
        tools:targetApi="31"
        android:name=".utils.MyApp">
        <activity
            android:name=".activitys.ListaOrdenesActivity"
            android:exported="false" />
        <activity android:name=".activitys.FaceDetectionActivity" />
        <activity android:name=".activitys.MainActivity" />
        <activity android:name=".activitys.ReporteFallaActivity" />
        <activity android:name=".activitys.SolicitaRefaccionActivity" />
        <activity android:name=".activitys.ManoObraActivity" />
        <activity android:name=".activitys.ValidaOrdenActivity" />
        <activity
            android:name=".activitys.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>